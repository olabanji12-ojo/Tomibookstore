// BookCard — components/home/BookCard.tsx
// TODO: Build this component

interface BookCardProps {
  title: string;
}

const BookCard = ({ title }: BookCardProps) => {
  return <div>{title}</div>;
};

export default BookCard;
